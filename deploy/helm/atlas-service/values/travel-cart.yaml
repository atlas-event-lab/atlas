# travel-cart-service — pre-booking cart, REST-only (no Kafka). CPU HPA, 1–3 replicas.
# Frankfurter FX is an external API (hardcoded default), so no env needed for it.
# Deploy:  helm upgrade --install travel-cart-service ../atlas-service -f travel-cart.yaml -n atlas-apps

image:
  repository: ghcr.io/atlas-event-lab/atlas-travel-cart-service
  tag: "sha-124a9f545a1e10accb438a51f76d1de5849b6fa3"
autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 70
# Spread the replicas across nodes — pointless at 1 pod, meaningful now that it can reach 3.
podAntiAffinity:
  enabled: true
config:
  DB_URL: jdbc:postgresql://atlas-pg-pooler-rw.atlas-data:5432/travel_cart_db?prepareThreshold=0
  FLYWAY_DB_URL: jdbc:postgresql://atlas-pg-rw.atlas-data:5432/travel_cart_db
  # EXPERIMENTO (rama exp/pg-resources-pool1): Hikari alineado con el pooler.
  # Nombres en formato relaxed-binding de Spring: punto -> "_", GUION ELIMINADO, mayusculas.
  # Un guion aqui haria que kubelet descarte la variable (no es un C_IDENTIFIER valido).
  SPRING_DATASOURCE_HIKARI_MAXIMUMPOOLSIZE: "8"
  SPRING_DATASOURCE_HIKARI_MINIMUMIDLE: "2"
  SPRING_FLYWAY_BASELINE_ON_MIGRATE: "true"
  SPRING_FLYWAY_BASELINE_VERSION: "0"
  REDIS_HOST: redis.atlas-data
  REDIS_PORT: "6379"
envSecret:
  enabled: true
  name: travel-cart-secret # keys: DB_USERNAME, DB_PASSWORD (SealedSecret)
extraEnv:
  - name: KEYCLOAK_ISSUER_URI
    valueFrom:
      secretKeyRef:
        name: atlas-issuer
        key: KEYCLOAK_ISSUER_URI
