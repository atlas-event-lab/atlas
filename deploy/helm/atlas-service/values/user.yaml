# user-service — self-service profile, REST-only (no Kafka). Baseline: replica 1, no HPA.
# Deploy:  helm upgrade --install user-service ../atlas-service -f user.yaml -n atlas-apps

image:
  repository: ghcr.io/atlas-event-lab/atlas-user-service
  tag: "sha-aba2f6ebf0a2e98e8fb421fccb8f078d5e2db7a3"
config:
  DB_URL: jdbc:postgresql://atlas-pg-pooler-rw.atlas-data:5432/user_db?prepareThreshold=0
  FLYWAY_DB_URL: jdbc:postgresql://atlas-pg-rw.atlas-data:5432/user_db
  SPRING_FLYWAY_BASELINE_ON_MIGRATE: "true"
  SPRING_FLYWAY_BASELINE_VERSION: "0"
envSecret:
  enabled: true
  name: user-secret # keys: DB_USERNAME, DB_PASSWORD (SealedSecret)
# Public Keycloak issuer (the `iss` browser-login JWTs carry; validated by this service).
# Injected from the `atlas-issuer` Secret so the public host/IP is NOT committed. The Secret
# is a per-cluster prerequisite — see DEPLOYMENT-RUNBOOK.md § "App secret — atlas-issuer".
extraEnv:
  - name: KEYCLOAK_ISSUER_URI
    valueFrom:
      secretKeyRef:
        name: atlas-issuer
        key: KEYCLOAK_ISSUER_URI
