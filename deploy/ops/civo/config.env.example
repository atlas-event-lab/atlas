# Atlas — Civo lifecycle config (template). Copy to config.env (gitignored).
#   cp config.env.example config.env
# NOTE: the Civo API token is NOT set here — export it in your shell instead:
#   export CIVO_TOKEN="your-civo-api-key"
CLUSTER_NAME="atlas-civo"
# TF_DIR="../../cluster/civo/terraform"   # override only if you moved the module
