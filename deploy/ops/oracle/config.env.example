# Atlas — OCI ops configuration (template).
# Copy to config.env (gitignored) and set your own node pool OCID.
#
#   cp config.env.example config.env
#
# Find it with:
#   oci ce node-pool list --compartment-id <compartment-ocid> \
#     --query 'data[].{name:name,id:id}' --output table

NODEPOOL_OCID=""
